<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@android:id/background">
        <shape>
            <solid android:color="#CCCCCC" />
        </shape>
    </item>
    <item android:id="@android:id/progress">
        <clip>
            <shape>
                <solid android:color="@color/secondary" /> <!-- Ganti warna sesuai kebutuhan -->
                <corners android:radius="30dp" /> <!-- Ganti radius sesuai kebutuhan -->
            </shape>
        </clip>
    </item>
</layer-list>